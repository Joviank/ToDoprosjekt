#cloud-config
packages:
  - git
  - docker.io
  - docker-compose

runcmd:
  - mkdir -p /opt/app
  - git clone https://github.com/Joviank/ToDoprosjekt.git /opt/app
  - cd /opt/app && docker-compose up -d
